# Hone — Claude Instructions

## Before every commit

Run `bun run lint` and fix all errors before staging. If biome reports format
issues, use `bunx biome format --write <file>` to auto-fix, then re-run lint
to confirm clean.
