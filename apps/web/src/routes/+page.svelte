<main style="padding: calc(var(--safe-top) + var(--space-6)) var(--space-4) calc(var(--safe-bottom) + var(--space-6));">
  <section
    style="max-width: 40rem; margin: 0 auto; padding: var(--space-6); background: var(--color-surface-card); border: 1px solid var(--color-border-subtle); border-radius: var(--radius-lg); box-shadow: var(--shadow-sm);"
  >
    <p style="margin: 0 0 var(--space-2); color: var(--color-accent); font-weight: 600;">Hone MVP</p>
    <h1 style="margin: 0 0 var(--space-4); font-size: clamp(2rem, 8vw, 3.5rem); line-height: 1.05;">
      Sprint 0 foundation scaffold
    </h1>
    <p style="margin: 0; color: var(--color-text-secondary); line-height: 1.6;">
      The app shell, PWA wiring, and Svelte 5 runes contexts are ready for the next implementation sprints.
    </p>
  </section>
</main>